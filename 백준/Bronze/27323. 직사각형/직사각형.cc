#include <iostream>

using namespace std;

int main(void) {
    int width, height;
    
    cin >> width >> height;
    
    int area = width * height;
    cout << area << "\n";
    
    return 0;
}